import { render } from '@testing-library/react';
import App from "./App";

// Tests that the component renders without errors
it('test render without errors', () => {
    // const { container } = render(<App />);
    // expect(container).toBeInTheDocument();
});